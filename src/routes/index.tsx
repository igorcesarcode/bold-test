import { NavigationContainer } from "@react-navigation/native";
import { Stacks } from "./stacks.routes";

export const Routes = () => {
  return (
    <NavigationContainer>
      <Stacks />
    </NavigationContainer>
  );
};
